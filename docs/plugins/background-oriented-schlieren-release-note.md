# BOS target release note

Fresnel now includes an experimental Background-Oriented Schlieren measurement plugin for generating seeded random-dot capture targets. The graphical editor can preview and export a bounded binary PNG, and target parameters can be stored in a canonical `.fresnel` job. This release does not yet include image capture or displacement analysis.
